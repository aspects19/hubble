<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= username %>'s Profile</title>
    <%- include('partials/head', { title: `${username}'s Profile` }) %>
  </head>
  <body>
    <div class="container mt-5">
      <h1><%= displayName %></h1> <!-- Display the hardcoded display name -->
      <h6><strong>@<%= username %></strong></h6> <!-- Display the username -->
    </div>
  </body>
</html>
